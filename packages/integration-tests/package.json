{
  "name": "@browserflow/integration-tests",
  "version": "0.0.1",
  "description": "Integration tests for BrowserFlow CLI pipeline",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@browserflow/cli": "workspace:*",
    "@browserflow/core": "workspace:*",
    "@browserflow/exploration": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "bun-types": "^1.1.0",
    "typescript": "^5.3.0"
  }
}
